{
  "id": "37d4bcf1",
  "title": "Remove git worktree management from Zed",
  "tags": [
    "zed-custom",
    "worktree-mgmt"
  ],
  "status": "closed",
  "created_at": "2026-06-12T22:59:23.903Z"
}

# Remove git worktree management — DONE

## Final commit range
`6a33aed656..5920e3173c` (9 commits, message prefix `TODO-37d4bcf1(worktree-mgmt):`):

1. `6a33aed656` — remove worktree picker button from title bar (`render_worktree_and_branch` → `render_branch`, dropped `linked_worktree_name` plumbing)
2. `1b80fd5b50` — delete `worktree_picker.rs`/`worktree_service.rs`/`worktree_names.rs` + action registrations in `git_ui.rs`
3. `df437ccd65` — remove keybindings (`git::Worktree`, `worktree_picker::DeleteWorktree`/`ForceDeleteWorktree` + `WorktreePicker` context blocks) from all three default keymaps
4. `d9e3494c2f` — remove `zed_actions` `CreateWorktree`/`SwitchWorktree`/`OpenWorktreeInNewWindow`/`NewWorktreeBranchTarget`/`git::Worktree`; remove `use_new_worktree`/`worktree_name`/`base_ref` from agent `create_thread` tool + `SiblingThreadRequest`; agent panel sibling threads no longer create worktrees; deleted worktree_service-dependent tests in agent_panel.rs
5. `55c1a85750` — remove `Workspace::run_create_worktree_tasks`, `ActiveWorktreeCreation`, `PreviousWorkspaceState`, `capture_state_for_worktree_switch`, `Event::WorktreeCreationChanged`; remove `worktree_picker` namespace from zed.rs test
6. `579ca017d1` — remove `TaskHook` enum, `TaskTemplate.hooks` field, `Inventory::templates_with_hooks`
7. `932c1ce315` — docs: rewrote git.md worktrees section (open-only), removed tasks.md Hooks section, parallel-agents.md/agent-panel.md/remote-development.md picker references, all-settings.md `git.worktree_directory` section
8. `ce311d2847` — `Sidebar::roots_to_archive_for_paths` now returns `Vec::new()`: archiving a thread never persists/removes a git worktree from disk
9. `5920e3173c` — deleted sidebar tests asserting archival removes worktrees from disk

## Design decisions
- **Kept** (per strategy): `crates/project` git_store worktree plumbing (`create_worktree`/`remove_worktree` repo APIs, `GitCreateWorktree` proto, `worktrees_directory_for_repo`, `git.worktree_directory` setting in settings_content), opening an existing git worktree as a project (`bd03dea296` behavior), `ZED_MAIN_GIT_WORKTREE` task variable, linked-worktree labels in the sidebar.
- **Kept** `agent_ui/src/thread_worktree_archive.rs` restore path (`restore_worktree_via_git` + cleanup): threads archived by *older builds* with worktrees on disk can still be restored. New archivals never touch worktrees because `roots_to_archive_for_paths` is stubbed to empty. The Phase-3 agent (TODO-a28ec443) can delete the whole module if thread archival goes away.

## Verification
- `cargo check` clean: zed_actions, task, project, workspace, title_bar, git_ui, agent, agent_ui, sidebar
- `./script/clippy -p git_ui -p title_bar -p workspace -p task -p zed_actions` clean
- Tests pass: git_ui (90), workspace tasks (7), task (23)
- Full-workspace build currently blocked only by TODO-a28ec443's in-flight uncommitted text-thread work (`AgentPanel::load` signature drift in sidebar) — unrelated to this todo.
